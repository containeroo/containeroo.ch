{
  "name": "containeroo.ch",
  "private": true,
  "scripts": {
    "build": "hugo",
    "dev": "hugo server",
    "check:worker": "npm run build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "postcss-cli": "^12.0.0",
    "wrangler": "4.129.0"
  }
}
